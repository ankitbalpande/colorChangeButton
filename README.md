# colorChangeButton
A simple JavaScript project that changes the webpage background color to a random RGB value on button click. It also dynamically updates the button text color, demonstrating basic DOM manipulation, event handling, and random color generation using JavaScript.
